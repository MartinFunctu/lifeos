---
trigger: always_on
---

dont implement api endpoint handlers directly in routes.ts, but put it in handlers.ts;
always test your implementation but not in browser, just analyze;
always analyze as much as you need to provide the best implementation;
if you are uncertain, ask user;
dont test in browser;
all own react components and hooks store in client/src/@, here analyze in which folder it belongs correctly;
my OS is Windows 11;
when implementing anything on backend and you want to log something, user logger;
always you i18n translations, not hardcoded strings;
always add translations located in shared/translations.ts;